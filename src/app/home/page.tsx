import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Home",
    description: "Teste home",
};

const Home = () => {
    return (
        <>
        </>
    )
}

export default Home